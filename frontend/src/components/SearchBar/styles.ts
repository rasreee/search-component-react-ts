import styled from 'styled-components';

export const Container = styled.div`
    display: flex;
    flex-direction: column;
`;

export const InputRow = styled(Container)`
    flex-direction: row;
`;
